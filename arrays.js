const arrayNumbers = [1, 2, 3, 4, 5];
const arrayNumbers2 = [6, 7, 8, 9, 10];

// Método concat une duas listas.
// const allNumbers = arrayNumbers.concat(arrayNumbers2);
// console.log(allNumbers);

// const text = "jeremias lindo";
// // Transforma o texto em caixa alta
// console.log(text.toUpperCase());
// // Retorna o índice que corresponde a posição do caracter
// console.log(text.indexOf("e"));
// console.log(arrayNumbers);

// Objects

// diferentemente dos arrays, que é uma lista que contém itens. Podemos descrever os objetos como a descrição do item

let person = {
  name: "Jeremias",
  age: 26,
};
person.lastname = "Verissimo";
console.log(person);
